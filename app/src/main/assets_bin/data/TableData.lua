local TableData = {}

TableData.uknown = "uknown"

TableData.Details = { }

TableData.Episode = { }

TableData.Genre = { }

TableData.library = { }

TableData.StreamUrl = { }

TableData.SourceStream = { }

return TableData